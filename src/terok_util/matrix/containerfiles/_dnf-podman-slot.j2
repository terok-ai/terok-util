{#-
SPDX-FileCopyrightText: 2026 Jiri Vyskocil
SPDX-License-Identifier: Apache-2.0
-#}
{#- Whole slot body for current Fedora (fedora43/fedora44): same package
    set, same nesting setup - only FROM differs per slot. -#}
{% include "_header.j2" %}

RUN dnf install -y \
    podman passt nftables dnsmasq slirp4netns fuse-overlayfs crun \
    python3 python3-pip \
    git curl bind-utils \
    $EXTRA_PACKAGES \
    && dnf clean all

{% include "_rootless-podman-setup.j2" %}

WORKDIR /src
